## What and why

<!-- The diff says what changed. Say why it needed to. -->

## Does this change a number?

<!-- Delete this section if it does not touch a measurement. -->

- What was it before, and what is it now?
- What was wrong with the old figure?
- Which test pins the new one?

## Checklist

- [ ] `node --test "test/*.test.ts"` passes
- [ ] `npx tsc --noEmit` is clean
- [ ] No new runtime dependency — Node still runs the source directly
- [ ] Exact and estimated figures stay clearly separated, and estimates are labelled
- [ ] No verdict claims a higher evidence tier than it reached
- [ ] Prevention-class claims remain protected from eviction
- [ ] Nothing new leaves the machine without an explicit opt-in
